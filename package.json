{
  "name": "uiu-health-care",
  "version": "1.0.0",
  "private": true,
  "description": "Student Healthcare Support Web Application for United International University",
  "scripts": {
    "client": "npm --prefix client run dev",
    "client:build": "npm --prefix client run build",
    "client:lint": "npm --prefix client run lint"
  }
}
